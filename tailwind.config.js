/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      // aggiungi qui eventuali estensioni (colori, font, spacing, ecc.)
    },
  },
  plugins: [
    // es: require('@tailwindcss/forms'), require('@tailwindcss/typography')
  ],
};
